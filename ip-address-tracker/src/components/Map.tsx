import * as React from "react";
import "../styles/search.map.scss";

// eslint-disable-next-line @typescript-eslint/no-empty-interface
interface IMapProps {}

const Map: React.FunctionComponent<IMapProps> = (props) => {
  return <h1>Map</h1>;
};

export default Map;
